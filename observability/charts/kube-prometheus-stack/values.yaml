# Built-in upstream values placeholder for prometheus-community/kube-prometheus-stack.
# Do not edit this file for environment-specific changes.
# Keep AKS/repository-specific overrides in custom-values.yaml.
#
# To refresh this file with the full upstream built-in values:
#   helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
#   helm repo update
#   helm show values prometheus-community/kube-prometheus-stack > observability/charts/kube-prometheus-stack/values.yaml
#
# Install using:
#   helm upgrade --install kube-prometheus-stack prometheus-community/kube-prometheus-stack \
#     --namespace observability \
#     --values observability/charts/kube-prometheus-stack/custom-values.yaml
